package appmeta

const (
	// Name is the CLI and TUI display name.
	Name = "lazycontainer"

	// ConfigDir is the application support directory for new installs.
	ConfigDir = "lazycontainer"

	// LegacyConfigDir is the pre-v0.3.0 config directory, kept for lookup.
	LegacyConfigDir = "lazycont"
)
